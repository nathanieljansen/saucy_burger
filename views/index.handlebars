<h1>WELCOME TO SAUCY BURGER!</h1>

<h2>Where the burgers are always saucy</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Your Name:</label>
    <input type="text" id="customerName" name="name">
  </div>
  <div class="form-group">
    <label for="ca">Your Burger:</label>
    <input type="text" id="burgerName" name="name">
  </div>

  {{!-- <div class="form-group">
    <label for="sleepy">Sleepy?</label>
    <br>
    <input type="radio" name="sleepy" value="1" checked> Ordered
    <br>
    <input type="radio" name="sleepy" value="0"> Awake
  </div> --}}

  <button type="submit">Place an Order</button>
</form>

<h2>New Burger Order!</h2>

<ul>
   {{#each orders}} {{#if orderPlaced}} {{> orders/orderStatus orderPlaced=true}} {{/if}} {{/each}}
  
</ul>

<h2>Burgers are Cooking!</h2>

<ul>
  {{#each orders}} {{#if cooking}} {{> orders/orderStatus orderPlaced=false}} {{/if}} {{/each}}
</ul>

<h2>Burgers are Served!</h2>

<ul>
  {{#each orders}} {{#if served}} {{> orders/orderStatus orderPlaced=false}} {{/if}} {{/each}}
</ul>

